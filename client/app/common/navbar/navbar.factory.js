let NavbarDataFactory = function () {
  let navbarData = {
      $: ''
  };

  let navbarFilter = () => {
    return navbarData;
  };

  return {
    navbarFilter
  };
};

export default NavbarDataFactory;
